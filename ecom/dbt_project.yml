name: ecom
version: '1.0.0'
config-version: 2

profile: ecom

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  ecom:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
